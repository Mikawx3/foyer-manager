{
  "name": "foyer-manager",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "predev": "npm run build -w @foyer/types",
    "dev": "concurrently -n web,api -c blue,green \"npm run dev -w @foyer/web\" \"npm run dev -w @foyer/api\"",
    "build": "npm run build -w @foyer/types && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
