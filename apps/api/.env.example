# Placeholders only. Real values belong in gitignored files, never in this repo.
#
#   cp .env.local.example .env.local              # local mode → local Postgres
#   cp .env.development.example .env.development  # cloud mode → Neon (or local foyer_dev)
#
# Production (Vercel) does not read these files. Set secrets in the Vercel project.
# See README.md for setup (createdb, migrate, dev scripts).
