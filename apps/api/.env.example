# Use environment-specific files instead of a single .env:
#
#   cp .env.local.example .env.local           # local mode  → foyer_local database
#   cp .env.development.example .env.development  # cloud mode → foyer_dev database
#
# See README.md for setup (createdb, migrate, dev scripts).
